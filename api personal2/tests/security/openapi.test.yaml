openapi: 3.0.3
info:
  title: Test Spec
  version: 1.0.0
paths:
  /echo:
    post:
      summary: Echo with validation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [foo]
              properties:
                foo:
                  type: string
      responses:
        "200":
          description: ok
