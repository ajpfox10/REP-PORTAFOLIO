// queues.ts - stub sin Redis
export const scanQueue = {
  add: async (..._args: any[]) => {},
};

export const ocrQueue = {
  add: async (..._args: any[]) => {},
};

export const aiQueue = {
  add: async (..._args: any[]) => {},
};

export const indexQueue = {
  add: async (..._args: any[]) => {},
};